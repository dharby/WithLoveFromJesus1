{
  "buildCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile && pnpm --filter @workspace/wlfj-landing build",
  "outputDirectory": "artifacts/wlfj-landing/dist/public",
  "installCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}